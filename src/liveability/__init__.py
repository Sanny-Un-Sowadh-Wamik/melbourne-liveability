"""liveability — Melbourne suburb liveability scoring (geospatial + PCA index).

Sub-packages:
    data      — real suburb boundaries (GeoJSON) + OSM POIs (Overpass, resilient)
    features  — the eight liveability dimensions
    scoring   — normalisation + PCA-weighted composite index
    viz       — Folium choropleth
"""

__version__ = "0.1.0"
